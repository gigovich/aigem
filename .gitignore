# Build output
/aigem
/aigem.exe
/mockmodel
/runner
/bin/
/dist/
/site/

# Built browser UI - filled by `make web`, never committed, so `go install`
# works with no node toolchain. See internal/web/assets.go.
/internal/web/dist/*
!/internal/web/dist/.gitkeep

# Local agent/editor state
/.claude
/.remember/
/.superpowers/
/.aigem/

# Editor and OS noise
*.swp
.DS_Store
